import numpy as np
import matplotlib.pyplot as plt
import cv2


'''
# numpy 이용
src = np.zeros((512, 512), dtype=np.uint8)

# matplotlib을 이용한 차트 그리기
plt.plot([0, 1, 2, 3, 4])
plt.show() # 나타난 창에서 x 버튼을 눌러야 정상 종료

# cv2를 이용한 numpy 자료형을 보여줌
cv2.imshow('src', src)
cv2.waitKey()
cv2.destroyAllWindows() # 아무키를 눌러야 정상 종료
'''
